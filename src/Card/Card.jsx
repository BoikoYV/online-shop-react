import React from 'react';
import PropTypes from 'prop-types';

const Card = props => {
    return (
        <li>
            
        </li>
    );
};

// Card.propTypes = {
    
// };

export default Card;