import React from 'react';
import PropTypes from 'prop-types';

const Favourites = props => {
    return (
        <div>
            here is favourites page
        </div>
    );
};

Favourites.propTypes = {
    
};

export default Favourites;